# Generation prompts

Claude system/user prompt templates for `packages/legal_generation`'s grounded-answer
contract (answer only from retrieved chunks, cite what's used, abstain when context is
insufficient — see the `grounded-answer-contract` skill). Nothing here yet —
`packages/legal_generation` has no implementation to template for.
