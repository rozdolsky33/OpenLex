# Experiments (placeholder)

Not used yet. Generation uses the Anthropic API directly (no fine-tuning); embeddings use an
off-the-shelf `sentence-transformers` model loaded in-process. Kept as a placeholder for the
future (e.g. embedding fine-tuning on the golden set). See
`docs/decisions/0001-monorepo-restructure.md`.
