# ML evaluation (placeholder)

For ad-hoc evaluation/analysis scripts (e.g. comparing retrieval configurations, embedding
model candidates) that are exploratory rather than a fixed regression suite. The fixed
golden-question regression suite lives in `tests/evaluation/` and runs in CI — see
`docs/decisions/0001-monorepo-restructure.md`.
