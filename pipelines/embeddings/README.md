# Embeddings (placeholder)

Computes `chunks.embedding` via `sentence-transformers` using
`settings.embedding_model_name` (see `ml/model_cards/bge-small-en-v1.5.md` for the
query-vs-passage prefix gotcha). Nothing here yet.
