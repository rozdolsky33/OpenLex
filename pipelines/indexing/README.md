# Indexing (placeholder)

Writes chunked + embedded documents into Postgres (`documents`/`chunks`), handling the
insert-new-`version`-not-mutate-`raw_snapshot` rule from the `openlex-data-model` skill.
Nothing here yet.
