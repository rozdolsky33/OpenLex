# NY case-law ingestion

Placeholder. NY Official Reports has no public API and blocks automated fetches, so this
source is not a live scraper — it will load a hand-curated `seed_cases.json` (real NY
landlord-tenant decisions, manually collected) rather than fetching anything at runtime.

Not yet created — see the `data-ingestion` agent and the `ny-open-legislation-api` skill
(for the sibling `ny_legislation/` adapter's conventions to mirror) before building this out.
