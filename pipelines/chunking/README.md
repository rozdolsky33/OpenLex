# Chunking (placeholder)

Splits normalized documents into `chunks` rows (`chunk_index`, `section_label`,
`heading_path`, `text`, `token_count`) — the operational wrapper around
`packages/legal_parsing`. Nothing here yet.
