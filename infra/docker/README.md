# Shared Docker assets (placeholder)

Not used yet — `apps/api/Dockerfile` and `apps/worker/Dockerfile` are self-contained today.
Put shared base images or production `docker-compose` overrides here if the per-app
Dockerfiles start duplicating significant setup.
