apiVersion: v1
kind: Service
metadata:
  name: openlex-api
  labels:
    app.kubernetes.io/name: openlex-api
spec:
  selector:
    app.kubernetes.io/name: openlex-api
  ports:
    - name: http
      port: 80
      targetPort: http
