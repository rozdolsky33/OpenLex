apiVersion: v1
kind: Service
metadata:
  name: openlex-web
  labels:
    app.kubernetes.io/name: openlex-web
spec:
  selector:
    app.kubernetes.io/name: openlex-web
  ports:
    - name: http
      port: 80
      targetPort: http
