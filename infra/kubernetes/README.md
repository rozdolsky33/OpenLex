# Kubernetes (placeholder)

Not used yet — the project runs entirely on local `docker-compose` today. When this becomes
real, follow the `base/` + `overlays/` (per-environment) convention. See
`docs/decisions/0001-monorepo-restructure.md` for why this is stubbed rather than built out
now.
