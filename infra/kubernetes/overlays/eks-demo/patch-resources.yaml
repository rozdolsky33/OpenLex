# infra/kubernetes/overlays/eks-demo/patch-resources.yaml
#
# Mirrors infra/kubernetes/overlays/kind/patch-resources.yaml's nodeSelector/anti-affinity
# pattern, adjusted for a real multi-AZ cluster (see infra/terraform/eks.tf's `apps` node
# group — fixed at 2 nodes, one per AZ): topologyKey is the AZ label, not the per-node hostname
# label. Anti-affinity is `preferred` (soft), same as kind — a hard AZ requirement left the 2nd
# replica Pending on t4g.medium's low max-pods (see the podAntiAffinity comment below). To get
# strict one-per-AZ HA back, raise the apps node group's max-pods (ENI prefix delegation) or use
# a larger instance type, then flip this back to `required`.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: openlex-api
spec:
  replicas: 2
  template:
    spec:
      nodeSelector:
        openlex.dev/workload: apps
      affinity:
        podAntiAffinity:
          # `preferred` (soft), not `required`: with t4g.medium's low max-pods, a strict
          # one-replica-per-AZ leaves the 2nd replica Pending ("Too many pods") when the other
          # AZ's node is at its pod ceiling. Soft still spreads replicas across AZs when there's
          # room, but lets the 2nd one co-locate rather than never scheduling.
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchLabels:
                    app.kubernetes.io/name: openlex-api
                topologyKey: topology.kubernetes.io/zone
      containers:
        - name: api
          imagePullPolicy: IfNotPresent
          # CORS: the browser SPA is served from https://app.openlex.arwest.dev (CloudFront), a
          # different origin than the API (https://api.openlex.arwest.dev), so the API must allow
          # it explicitly. openlex_shared.config.cors_allow_origins defaults to
          # ["http://localhost:5173"] (local dev only); without this override every browser call
          # from the deployed web app fails its CORS preflight ("Disallowed CORS origin"), which
          # the SPA surfaces as the generic "Something went wrong" (a blocked request never
          # returns a 401, so it isn't shown as bad credentials). pydantic-settings parses this
          # list field as JSON. localhost kept so a local web build can point at the prod API.
          env:
            - name: CORS_ALLOW_ORIGINS
              value: '["https://app.openlex.arwest.dev","http://localhost:5173"]'
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: 1000m
              memory: 1Gi
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 60
            periodSeconds: 10
            failureThreshold: 6
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 90
            periodSeconds: 15
            failureThreshold: 5
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: openlex-worker
spec:
  template:
    spec:
      nodeSelector:
        openlex.dev/workload: apps
      containers:
        - name: worker
          imagePullPolicy: IfNotPresent
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: 1000m
              memory: 1Gi
