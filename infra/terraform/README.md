# Terraform (placeholder)

Not used yet — the project runs entirely on local `docker-compose` today. Add Terraform here
when OpenLex needs to provision real infrastructure (managed Postgres, container hosting,
secrets management, etc.). See `docs/decisions/0001-monorepo-restructure.md` for why this is
stubbed rather than built out now.
