# Must stay a browser-reachable URL (e.g. http://localhost:8000), never a Docker-internal
# service name like http://api:8000 -- API calls are made from the browser via fetch, not
# from inside this container, so container DNS names don't resolve for the client.
VITE_API_BASE_URL=http://localhost:8000
