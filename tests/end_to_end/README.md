# End-to-end tests

Full-stack tests driving the running `docker-compose` stack (e.g. hitting `apps/api` over
HTTP and asserting on the response). Nothing here yet — there's no running API to test
against until `apps/api/src/openlex_api/main.py` exists.
